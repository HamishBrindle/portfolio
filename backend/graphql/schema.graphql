# import * from './generated/prisma.graphql'

type Mutation {
    createProject(
        name: String!
        description: String!
        page: PageCreateOneInput!
    ): Project!

    deleteProject(id: String): Project!
}

type Query {
    projects: [Project!]!
}