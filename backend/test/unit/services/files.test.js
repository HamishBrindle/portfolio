import { request } from 'graphql-request';

import filesService from '../../../api/services/files';

describe('Files Services: Upload', () => {
  test('can upload a file to tmp directory', async () => {
    // Upload a file to the .tmp directory
    // ...
    return true;
  })
});