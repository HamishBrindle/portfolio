enum Permisson {
    ADMIN
    USER
    PERMISSON_UPDATE
}

enum Shade {
    LIGHT
    DARK
}

enum Shape {
    JAGGED
    ROUND
    SQUARE
}

enum ColorType {
    PRIMARY
    SECONDARY
    SUCCESS
    DANGER
    WARNING
    INFO
    LIGHT
    DARK
    MUTED
    WHITE
    MISC
}

enum ProjectImageType {
    PRIMARY
    HEADER
    SECONDARY
    LAZY
}